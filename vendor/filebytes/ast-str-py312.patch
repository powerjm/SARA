--- a/setup.py
+++ b/setup.py
@@ -7,7 +7,7 @@ def extractMetaInfo(src):
     info = {}
     a=ast.parse(src)
     for e in a.body:
-        if isinstance(e, ast.Assign) and isinstance(e.value, ast.Str):
-            info[e.targets[0].id] = e.value.s
+        if isinstance(e, ast.Assign) and isinstance(e.value, ast.Constant):
+            info[e.targets[0].id] = e.value.value
     return info
